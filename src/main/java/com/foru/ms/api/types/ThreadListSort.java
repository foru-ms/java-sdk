/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.foru.ms.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ThreadListSort {
    public static final ThreadListSort ACTIVITY = new ThreadListSort(Value.ACTIVITY, "activity");

    public static final ThreadListSort OLDEST = new ThreadListSort(Value.OLDEST, "oldest");

    public static final ThreadListSort NEWEST = new ThreadListSort(Value.NEWEST, "newest");

    private final Value value;

    private final String string;

    ThreadListSort(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ThreadListSort && this.string.equals(((ThreadListSort) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case ACTIVITY:
                return visitor.visitActivity();
            case OLDEST:
                return visitor.visitOldest();
            case NEWEST:
                return visitor.visitNewest();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ThreadListSort valueOf(String value) {
        switch (value) {
            case "activity":
                return ACTIVITY;
            case "oldest":
                return OLDEST;
            case "newest":
                return NEWEST;
            default:
                return new ThreadListSort(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        NEWEST,

        OLDEST,

        ACTIVITY,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitNewest();

        T visitOldest();

        T visitActivity();

        T visitUnknown(String unknownType);
    }
}
