/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.foru.ms.api.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class RoleListSort {
    public static final RoleListSort NAME = new RoleListSort(Value.NAME, "name");

    public static final RoleListSort OLDEST = new RoleListSort(Value.OLDEST, "oldest");

    public static final RoleListSort NEWEST = new RoleListSort(Value.NEWEST, "newest");

    private final Value value;

    private final String string;

    RoleListSort(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof RoleListSort && this.string.equals(((RoleListSort) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case NAME:
                return visitor.visitName();
            case OLDEST:
                return visitor.visitOldest();
            case NEWEST:
                return visitor.visitNewest();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static RoleListSort valueOf(String value) {
        switch (value) {
            case "name":
                return NAME;
            case "oldest":
                return OLDEST;
            case "newest":
                return NEWEST;
            default:
                return new RoleListSort(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        NEWEST,

        OLDEST,

        NAME,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitNewest();

        T visitOldest();

        T visitName();

        T visitUnknown(String unknownType);
    }
}
